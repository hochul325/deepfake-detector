name: "wav2vec2-audio-deepfake-detector-v5v6"
version: "6.0.0"
modality: "audio"

preprocessing:
  sample_rate: 16000
  duration_sec: 6
  normalize: true

model:
  num_classes: 2
  weights_file: "model.safetensors"
