name: "wav2vec2-audio-deepfake-detector-gas2"
version: "10.1.0"
modality: "audio"

preprocessing:
  sample_rate: 16000
  duration_seconds: 6.0

model:
  num_classes: 2
  weights_file: "model.safetensors"
